# PJAMASC Website

## Installation Instructions
1. Install Apache 2.
1b. Set the document root to `<project_root>/public`.
2. Install PHP version 7.2 or higher.
4. Install MySQL.
4b. Run `pjamasc.sql`.
5. Install [Composer](https://getcomposer.org/download/).
6. Run `composer install` in the project root.
